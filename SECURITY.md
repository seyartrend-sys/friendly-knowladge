# الأمان والخصوصية

نُسُج MVP مصمم لمساحة شخصية لمستخدم واحد.

## ما هو مطبق

- بوابة دخول بكلمة مرور واحدة للتطبيق.
- جلسة موقّعة بـ HMAC داخل Cookie من نوع `HttpOnly` و`SameSite=Strict`.
- مقارنة كلمة المرور بصورة ثابتة التوقيت.
- حدّ بسيط لمحاولات الدخول المتكررة.
- حماية جميع واجهات المعرفة والبحث والمحادثة من الوصول غير المصرح.
- متغيرات الأسرار غير مضمّنة في المستودع.
- عناوين أمان HTTP تمنع التضمين وتقلل تسريب السياق.
- قاعدة Render غير متاحة مباشرة من الإنترنت في `render.yaml`.

## إعداد إنتاج صحيح

1. عيّن `APP_PASSWORD` إلى كلمة قوية وفريدة.
2. عيّن `AUTH_SECRET` إلى قيمة عشوائية لا تقل عن 32 بايت.
3. لا تضع `DATABASE_URL` أو `AI_API_KEY` في Git.
4. حدّث الاعتماديات بانتظام وراقب تنبيهات GitHub.
5. انسخ قاعدة البيانات احتياطياً قبل أي ترحيل يدوي.

## حدود النسخة الحالية

- المصادقة لمستخدم واحد وليست نظام حسابات متعدد المستخدمين.
- حد المحاولات محفوظ في ذاكرة الخادم؛ للإطلاق العام أضف Redis أو خدمة rate limiting.
- لا توجد مرفقات أو رفع ملفات في هذا الإصدار.
- لا ينبغي استخدام النسخة الحالية لمعلومات صحية أو مالية أو أسرار عالية الحساسية.

للانتقال إلى منتج متعدد المستخدمين: استخدم مزود هوية موثوقاً، وأضف `user_id` إلزامياً إلى كل استعلام، وسياسات وصول على مستوى الصفوف، وسجل تدقيق.
